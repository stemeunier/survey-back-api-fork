name: Call workflow Python unittest application
on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev
jobs:
  call-workflow:
    uses: optittm/common-ci/.github/workflows/unitest.yml@main
